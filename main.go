package main

import (
	"bufio"
	"os"
)

func main() {
	scanner := bufio.NewScanner(os.Stdin)

	// read input
	// read key
	// rev option (enc)

	// brute key (known phrase)
	// keylen
	// kewwords

	// construct offsets
	// perform vigenere
	// write out
}
